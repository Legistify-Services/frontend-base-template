import React from "react";
import { Card, CardHeader } from "reactstrap";

const AddContract = () => {
  return (
    <Card className="default_card_height">
      <CardHeader className="page_main_title">Add Contract</CardHeader>
    </Card>
  );
};

export default AddContract;
